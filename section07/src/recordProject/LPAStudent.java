package section07.src.recordProject;

// record header
public record LPAStudent(String id, String name, String dateOfBirth, String classList) {
    // Notice () after name of record. Once parameters are in, we are ready to use record
    // fields declared here are private and final

}
